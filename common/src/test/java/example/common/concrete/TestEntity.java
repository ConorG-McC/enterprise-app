package example.common.concrete;

import example.common.domain.Entity;
import example.common.domain.Identity;

public class TestEntity extends Entity {
    public TestEntity(Identity id) {
        super(id);
    }
}
